# =============================================================================
# Async Boardgame Service - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your deployment
# SECURITY WARNING: Never commit .env files to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------

# PostgreSQL password for the boardgame user
# REQUIRED: Change this to a strong password in production!
# Use a password generator for production deployments
# Minimum 16 characters recommended with mixed case, numbers, and symbols
DB_PASSWORD=changeme_in_production

# Database connection pool size
# Default: 10 connections
# Increase for high-traffic deployments, decrease for resource-constrained environments
# DB_POOL_SIZE=10

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

# HTTP server port
# Default: 3000
# Change if port 3000 is already in use on your system
PORT=3001

# Node environment
# Options: development, production, test
# Default: production
# Use 'development' for local development with verbose logging
# NODE_ENV=production

# -----------------------------------------------------------------------------
# Authentication Configuration (Clerk)
# -----------------------------------------------------------------------------

# Enable/disable authentication
# Options: true, false
# Default: false (disabled for local development)
# Clerk API Keys
# REQUIRED for authentication
# Get these from your Clerk dashboard: https://dashboard.clerk.com
# 
# 1. Create a Clerk account at https://clerk.com
# 2. Create a new application in the Clerk dashboard
# 3. Configure OAuth providers (Discord, Google, GitHub) in the dashboard
# 4. Copy the keys from: Configure > API Keys
#
# Publishable Key: Safe to expose in client-side code (starts with pk_)
# Secret Key: MUST be kept secret, used for server-side operations (starts with sk_)
CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Clerk Configuration Notes:
# - Publishable key is used by both backend and web client
# - Secret key is ONLY used by the backend (never expose in client code)
# - In production, use live keys (pk_live_* and sk_live_*) instead of test keys
# - Configure allowed redirect URLs in Clerk dashboard for OAuth callbacks
# - Supported OAuth providers: Discord, Google, GitHub (configure in Clerk dashboard)

# -----------------------------------------------------------------------------
# Admin Authorization Configuration
# -----------------------------------------------------------------------------

# Admin User IDs
# Comma-separated list of user IDs that have admin access
# These IDs are the internal player IDs (not Clerk IDs)
# Example: ADMIN_USER_IDS=player_abc123,player_def456
# Leave empty if no admin users are configured
# ADMIN_USER_IDS=

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------

# Log level
# Options: debug, info, warn, error
# Default: info
# Use 'debug' for troubleshooting, 'warn' or 'error' for production
# LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Production Deployment Notes
# -----------------------------------------------------------------------------
# 
# 1. SECURITY: Always change DB_PASSWORD to a strong, unique password
# 2. BACKUPS: Regularly backup the postgres-data Docker volume
# 3. UPDATES: Keep Docker images updated for security patches
# 4. MONITORING: Monitor logs with: docker-compose logs -f
# 5. FIREWALL: Ensure only port 3000 is exposed to the network
# 6. HTTPS: Use a reverse proxy (nginx/traefik) for HTTPS in production
# 7. RESOURCES: Monitor container resource usage with: docker stats
#
# For detailed deployment instructions, see DEPLOYMENT.md
# =============================================================================
